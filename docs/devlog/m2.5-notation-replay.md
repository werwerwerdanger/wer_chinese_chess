# M2.5 打谱模式 开发日志

## 2026-09-12 11:50 ~ 12:10（代码） / 21:24（推送）

### 做了什么
1. engine/src/notation.ts — 中文纵线记谱（"炮二平五"式）
2. adapter 接口扩展：getFen / loadFen / moveNumber / seekTo
3. game.ts 回放模式：viewIndex 游标、着法列表点击跳转、⏮◀▶⏭ 导航
4. UI：FEN 复制/载入、棋谱导出
5. PNG 棋子素材：scripts/gen_pieces.py 用 PIL 生成（楷体+径向渐变+双圈描边+阴影）

### 记谱规则实现要点（>>报告素材："数据结构"章节）
- 红方纵线：col 8 = 一线 … col 0 = 九线（中文数字，从红方右手数）
- 黑方纵线：col 8 = 1线 … col 0 = 9线（阿拉伯数字，从**黑方**右手数 = 画面右侧）
- 动作：平（横移）/进/退（纵向，红向上为进、黑向下为进）
- 纵向多格：红"车一进二"（中文数字步数）、黑"车1进2"
- 斜走子（马象仕）：进/退 + 目标纵线
- 同列同名子消歧：前/后缀（红方 row 小者为前，黑方相反）
- 未过河兵不消歧（同列双未过河兵不可能同时有攻击选择）

### 踩坑记录
**坑1：黑方纵线方向想当然**
- 现象：测试期望"卒3进1"，引擎输出"卒7进1"
- 根因：我把黑方纵线当成"从画面左侧数"（col 0=1线），但黑方坐对面，它的右手是画面右侧（col 8=1线）
- 修复：BLACK_NUMS 数组倒序
- 教训：**视角转换类逻辑必须以"该方的视角"写注释和测试**，画个小图确认

**坑2：测试 FEN 构造将帅照面**
- 现象："车一进二"测试找不到走子（no move）
- 根因：构造的 FEN 里红帅黑将同列且中间无子——这局面本身就非法，所有走子被"照面过滤"干掉了
- 修复：两将错开列
- 教训：手工构造测试局面时，先检查局面自身的合法性（照面/被将军），否则测的不是你以为的东西

**坑3：棋子名用繁体**
- 现象：记谱输出"傌八进七"，标准是"马八进七"
- 根因：PIECE_NAMES 沿用了"红方专用字"（傌俥相仕兵）图好看，但标准记谱红黑同用简体（马车炮）
- 修复：统一简体，红黑只靠颜色区分

### 测试结果
24/24 全绿（18 引擎规则 + 6 记谱用例：炮二平五/马八进七/兵三进一/卒7进1/炮8平5/车一进二）

### 素材管线（>>报告素材："UI实现"章节）
- PIL 生成 14 张 256×256 PNG（红7黑7），径向渐变象牙底 + 双色环描边 + 高斯模糊投影 + 楷体字
- 放 web/public/pieces/，前端 drawImage 缩放绘制
- **待办**：renderer.ts 还是手绘棋子，PNG 接入未完成（下一步）

## 2026-09-12 21:55 PNG 素材接入 renderer（M2.5 收尾）

### 实现
- renderer.ts：`preloadPieceImages()` 预加载 14 张 PNG，`pieceImages` Map 按 Piece 编码缓存
- drawPiece 双路径：素材就绪 → drawImage（cell*0.92）；未就绪/单张失败 → Canvas 手绘兜底（优雅降级）
- 回调链：setOnImagesReady → controller.refresh() → 重绘一帧
- main.ts 启动时先建 controller 再触发预加载，避免回调早于 controller 注册

### 验证
- build 通过；dev server 页面 200、/pieces/*.png 200
- curl 检查坑：`curl -o /dev/null` 在沙箱对某些响应返回 000/exit 23（写盘权限），实际服务正常，用 `-I` 或直接抓 body 验证
